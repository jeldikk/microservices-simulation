
-- drop database if exists; we should never do this in production but okay in local development
DROP DATABASE IF EXISTS postgres_part1_fullcourse;


-- Create a new database inside postgre server
CREATE DATABASE postgres_part1_fullcourse;